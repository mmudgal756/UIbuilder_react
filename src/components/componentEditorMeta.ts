import buttonMeta from './ui/Button.meta';
import { PropMeta } from './propertyMeta';

const componentEditorMeta: Record<string, PropMeta[]> = {
  button: buttonMeta,
};

export default componentEditorMeta;
